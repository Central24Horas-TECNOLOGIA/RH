{
  "name": "conecta-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend estático do Conecta RH (vanilla JS, sem bundler). Este package.json existe para a infraestrutura de testes E2E (Playwright) e, opcionalmente, para o Vite como ferramenta de build/dev — o modo padrão suportado continua sendo servir os arquivos estáticos diretamente (ver README.md).",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:report": "playwright show-report",
    "dev": "vite",
    "build": "vite build"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "vite": "^5.4.11"
  }
}
